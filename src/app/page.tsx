import { Button } from '@mui/material';

const HomePage = () => {
    return (
        <div>
            <p>This is HomePage</p>
            <Button>Text</Button>
        </div>
    );
};

export default HomePage;
