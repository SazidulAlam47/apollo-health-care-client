import { Box } from '@mui/material';

const DashedLine = () => {
    return (
        <Box
            sx={{
                borderBottom: '1px dashed #ddd',
                marginBottom: 3,
            }}
        />
    );
};

export default DashedLine;
